name: Validate

permissions: {}

on:
  workflow_dispatch:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  validate:
    name: Validate
    uses: grafana/xk6/.github/workflows/extension-validate.yml@v1.4.13
    permissions:
      pages: write
      id-token: write
      contents: read
    with:
      go-version: "1.26.x"
      go-versions: '["1.25.x","1.26.x"]'
      golangci-lint-version: "v2.13.2"
      platforms: '["ubuntu-latest", "windows-latest", "macos-latest"]'
      k6-versions: '["v1.8.1", "v1.8.0"]'
      xk6-version: "1.4.13"
